{
  "name": "torbox-updater",
  "version": "0.1.0",
  "private": true,
  "description": "CLI/Webhook service to listen for Overseerr requests, search Prowlarr, and add torrents to TorBox",
  "main": "dist/index.js",
  "bin": {
    "torbox-updater": "dist/index.js"
  },
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js serve",
    "serve": "node dist/index.js serve"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "commander": "^14.0.2",
    "express": "^5.1.0",
    "node-torbox-api": "^0.1.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/node": "^24.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
